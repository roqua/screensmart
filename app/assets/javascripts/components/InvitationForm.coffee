{ DOM: { div } } = React

@InvitationForm = React.createClass
  displayName: 'InvitationForm'

  render: ->
    # @props.domains: a list of domains to choose from (only one can be chosen)
    div
      className: 'app'
      "Replace me!"

version: '3'
services:
  web:
    env_file: .env
    environment:
      - RAILS_ENV=production
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
      - .:/app
    ports:
      - "3000:3000"
